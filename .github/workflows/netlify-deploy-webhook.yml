name: "Run webhook"
on:
  schedule:
    - cron:  '* * * * *'    # At every minute

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Execute webhook for netlify deployment
        run: echo "testing every minute"
