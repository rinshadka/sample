name: "Run webhook webhoo webhoo webhoo webhoo webhoo"
on:
  push:

jobs:
  deploy:
    runs-on: debian-12
    steps:
      - name: Execute webhook for netlify deployment
        run: echo "testing every 6 hours"
